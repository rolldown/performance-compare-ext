{
  "name": "1000",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:vite": "vite build",
    "build:farm": "farm build",
    "build:rsbuild": "rsbuild build",
    "build:rolldown": "rolldown build --config rolldown.config.mjs",
    "build:esbuild": "esbuild --bundle --minify=false  --outdir=dist src/index.jsx"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {}
}
